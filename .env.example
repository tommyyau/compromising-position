# HIBP API key for email breach checks (Layer 3)
# Get one at https://haveibeenpwned.com/API/Key ($3.50/mo)
# Not needed for credential checks (Layer 1 + 2)
HIBP_API_KEY=

# Optional: path for append-only audit log (JSONL format)
# Only fingerprints are logged, never secrets
CP_AUDIT_LOG=
